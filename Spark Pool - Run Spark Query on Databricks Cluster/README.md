# Run Spark Query on Databricsk Cluster

Some code that connects to a Databricks cluster using JDBC and the provided Simba driver. You can read the results into a dataframe inside spark pools to do additional data prep and clensing operations.

The simba driver (*.jar file) needs to be added to the Synapse spark pool.

